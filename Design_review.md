# Design Review: SOLID Principles

| หลัก SOLID | ละเมิดหรือไม่ | จุดที่เกี่ยวข้อง (class/method) | อธิบาย/ผลกระทบ | ข้อเสนอปรับปรุง |
| :--- | :---: | :--- | :--- | :--- |
| **S (SRP)** | ไม่ละเมิด | `notifier`, `inventory` | มีหน้าที่แตกต่างกันชัดเจนและไม่ทำงานหลายอย่างในคลาสเดียว ผลกระทบไม่มี | ไม่มี |
| **O (OCP)** | ไม่ละเมิด | `notifier` | ใช้การแยก class เพื่อไม่ให้เวลาเพิ่มฟังก์ชันต้องกลับมาแก้ไขในคลาส | ไม่มี |
| **L (LSP)** | ไม่ละเมิด | `Invetory_no_context` class | คลาสลูกทำงานแทนคลาสแม่ได้ทุกกรณี | ไม่มี |
| **I (ISP)** | ละเมิด | `inventory` (บรรทัดที่ 90, 108, 125) | เป็นเรื่องเป็นไปไม่ได้ที่สินค้าที่จะเพิ่มจะเข้าระบบเป็น 0 โดยผู้ใช้และสินค้าจ่ายออกจะสามารถติดลบได้ | ใช้วิธีการอื่นในการตรวจสอบ |
| **D (DIP)** | ไม่ละเมิด | `notifier` | import Protocol ไม่ผูกติดกับ Low-level โดยตรง | ไม่มี |
